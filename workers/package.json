{
  "name": "mc-skin-creator-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "replay:analysis": "vitest run test/replayAnalysisArtifact.test.ts --reporter=verbose",
    "render:skin": "vitest run test/renderSkinArtifact.test.ts --reporter=verbose"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vitest": "^4.1.9",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4"
  }
}
