# Direct lower-face geometry / production reachability — 2026-09-13

## Result

Implemented an **inactive**, single-native-Gemma, bounded fail-open enrichment before the deterministic primary baseline. **Not ready to activate**: only one of three live geometry responses passed semantic validation, and the new prompt extension contained an image-numbering conflict discovered after dispatch. That conflict is corrected offline; the corrected prompt has **zero** live validations. No generation smoke was run, since its prerequisite was not met. No commit, push, deploy, or production flag activation occurred.

1. **Start:** `main`, HEAD/origin `f3c0732388c78bfe5796ba9b94c1c9dc7e897a3e`; working tree clean. Existing contour work was already committed at this actual starting point. All old artifacts retained; outputs are under `workers/evaluation-artifacts/direct-face-geometry-20260913/`.
2. **Ordinary production reachability before:** `runPhotoAnalysis → renderPrimaryBaseline → return` because production vars omit `SYNCHRONOUS_ENHANCEMENTS_ENABLED`. The previous focused geometry call was below this return, so ordinary requests never executed it.
3. **Likeness reachability:** source-supported categorical eye spacing/footprint/openness, brow distance/slope, mouth width/opening/expression still reach the quantizer without geometry. They are not calibrated continuous coordinates. Source-measured eye/brow/mouth anchors, measured nose placement/strength, independent cheek/jaw/chin boundaries and measured hair contours require identityGeometry; semantic nose/hair fallback is not calibration. The new, separately gated path places only geometry before the baseline; the heavy chain stays off, including when both flags are present.
4. **Fixed ratio root cause:** `deriveLegacyGeometryExtensions` computes `cheekWidth = visibleRight - visibleLeft`, `jawWidth = cheekWidth * 0.88`. The existing Gemini geometry wire requests no direct jaw/chin boundaries. All five stored cases therefore have ratio 0.88 (floating-point tolerance), not five independently calibrated jaws. Legacy derivation is retained and tagged `derived_geometry`, including rich-cache reparse.
5. **Direct wire:** `directLowerFaceContour.{cheek,jaw,chin}` each has nullable `left/right/y`, `evidence`, `confidence`. Full focused face/head geometry is retained in the same one-call schema. Current schema: **10,558 bytes / 138 properties / 138 required / depth 5 / 44 enum values / unsupported constructs 0**.
6. **Coordinate/evidence:** all direct lower-face boundaries use the tight-face crop only. Width is exactly `right-left`. Unknown requires null coordinates and confidence 0. No semantic/demographic/symmetry reconstruction. Invalid numbers, reversed rows, out-of-face bounds and lower width exceeding the visible upper width are rejected, not clamped. **Implementation mistake:** the initial extension said Image 1=tight/Image 2=head, conflicting with the inherited Image 0=tight/Image 1=head contract. It now refers to the existing tight-face contract without duplicating image numbers; a regression test locks this down. The initial measurements cannot validate the corrected contract.
7. **Internal geometry:** optional `directLowerFaceContour`; old caches remain parseable. Existing faceShape fields remain intact. Provider-shape validation is followed by direct-boundary validation, the existing rich geometry parser/semantic validator, and crop visibility handling.
8. **Provenance:** accepted per-cue traces use observed/inferred geometry. Unknown contributes no usable trace. Direct measurement names are in geometry diagnostics. Legacy faceShape derivation remains explicitly derived, and semantic fallback is distinct.
9. **Precedence:** usable direct cheek/jaw widths override legacy widths independently in FaceLayoutPlan. Chin has a separate optional quantized width. When a direct contract is present but a cue is unknown/unsafe, that cue does not borrow a fabricated legacy direct contour.
10. **Topology:** cheek/jaw/chin each spend zero or a bilateral 2 pixels; total ≤6. Jaw row and chin tip are separate grammar choices; broad chin needs no outline. Shades are complexion-relative, never transparent or fixed black. Existing hair/covering/glasses ownership blocks new contour pairs, without changing the ownership resolver. Landmarks retain priority.
11. **Offline diversity:** same cheek/different jaw and same jaw/different chin produce distinct layouts/topology. Direct-over-0.88, direct-over-semantic-square, historical compatibility, clipped/occluded chin-only suppression, no-geometry/no-fabrication and contradiction rejection pass. All five calibrated fixtures retain non-complexion eye/brow/mouth/nose/fringe pixel signatures, hair/outfit plans and glasses ownership. Existing legacy complexion cells are excluded from the immutable ownership comparison, since they are the intended replacement area; new contour cells may not replace other protected owners.
12. **Live measurements:** native Gemma returned schema-valid structured objects for all three calls; HTTP status is not separately exposed by the binding. Only short-red passed full semantic normalization. The table below shows measured values, not values repaired to fit the validator.

| Selected source | Cheek width | Jaw width | Chin width | Jaw/cheek | Chin/jaw | Semantic result | Latency |
|---|---:|---:|---:|---:|---:|---|---:|
| long-straight-hair | 0.50 | 0.51 | 0.16 | 1.0200 | 0.3137 | rejected: jaw wider than cheek | 14,667ms |
| short-hair-red-shirt | 0.45 | 0.40 | 0.25 | 0.8889 | 0.6250 | passed initial contract | 15,540ms |
| curly-hair | 0.50 | unknown | unknown | — | — | rejected: unknown jaw/chin confidence 0.4/0.3, not 0 | 24,699ms |

   Selection was dynamic: stored crop pairs, excluding glasses/coverings/missing mouth, sorted by face aspect, choosing min/median/max. No fixture-ID production branches. Sources were visually inspected. Curly has a turned/tilted face and hand near the jaw; no hidden boundary was filled in. Old crop clipping metadata is marked unknown where unavailable; historical default occlusion=false is not allowed to overwrite new provider occlusion.
13. **Jaw diversity:** raw ratios do not all equal 0.88, but one ratio is invalid and one missing. Therefore **usable three-case diversity is not established**.
14. **Chin diversity:** similarly, one valid ratio is insufficient. No diverse-source quality success is claimed.
15. **Latency:** 14.7s / 15.5s / 24.7s. The existing native binding runner is reused with a 25s soft cap; it stops awaiting and launches no replacement call. The binding has no abort facility in the reused path and may complete independently. Primary provider deadlines are unchanged (up to 90s combined); enrichment adds ≤25s provider wait, leaving roughly 35s of the frontend 150s window for local work. Three observations do not establish tail latency. Timeout was not increased after the probes.
16. **Fail-open:** mocked native timeout, provider failure and invalid schema all return `/api/generate` HTTP 200, ok=true and valid 64×64 PNG without new identityGeometry. Local crop errors are also caught. If enriched rendering fails, the original primary analysis is rendered instead. The primary provider classification/quota policy is unchanged; the extra call is accounted in the existing advisory usage meter.
17. **Production-equivalent live generation:** **not attempted**. The required meaningful three-case direct diversity did not pass. Local mock success proves wiring only, not live generation quality.
18. **External calls:** **Gemma geometry 3; Gemini 0; primary fallback 0; generation smoke 0; retry/fallback 0; portrait/image generation/critique/pairwise/evaluator 0**. No fourth call after finding the prompt mistake. Wrangler binding setup and documentation reads are not model inference calls.
19. **Atlas:** mock generation paths pass PNG decode, 64×64 dimensions and validateFinalAtlas. No live enriched final PNG is claimed.
20. **Regressions:** final selected offline Workers suite **49 files passed; 738 tests passed, 1 skipped**. Compact strict boundary 12/12, fabrication 0, craft 12/12, calibrated five atlas regression 0. Workers TypeScript, ESLint, git diff --check, Vite build and Wrangler dry-run pass. Secret scan found zero configured-secret matches. Frontend app TypeScript has an **unchanged, out-of-scope** error at `granite.config.ts:6`: `displayName` is not a property of `{ primaryColor: string }`; frontend/node config check passes. No unrelated fix was applied. This was not a blanket execution of old artifact-producing/live-canary tests, since their default outputs would overwrite historical evidence.
21. **Reliability core:** primary `gemini.ts` is byte-equivalent after removing the new five-line direct-entry wrapper (line-ending normalized comparison). Primary analysis/Compact/Gemma named schemas, fallback decision implementation, primary timeout rules, quota policy, image provider, critique, pairwise, head ownership resolver, craft/P5 validator and Wrangler vars have zero diff. The only provider architecture addition is the opt-in bounded enrichment before baseline. New flag remains absent in production config.
22. **Remaining geometry-quality blocker:** no coordinate-consistent, semantically valid three-source direct-contour acceptance evidence. Next iteration requires a separately authorized repeat of the corrected contract on suitable visible-boundary sources; only after success should the one production-equivalent generation smoke be considered. Do not activate yet.

## Evidence and hashes

- Initial live checkpoint: `workers/evaluation-artifacts/direct-face-geometry-20260913/live-2245/measurements.json` (unchanged after completion).
- Current offline contract: `workers/evaluation-artifacts/direct-face-geometry-20260913/offline/current-contract.json`.
- Final strict/craft replay: `workers/evaluation-artifacts/direct-face-geometry-20260913/offline/final-strict-boundary/coverage.json`.
- Compact equivalence replay: `workers/evaluation-artifacts/direct-face-geometry-20260913/offline/final-v3/`.
- Schema hash unchanged: `a39f13800df56ddaec1ce2ab44b671b6851773633626ba465ba17fade564a0dc`.
- Initial live prompt hash: `80e86b680153cac1ab244be08af22a2ce9542c8b1200a4a55ec4f9621ccec7ee`.
- Corrected offline-only prompt hash: `5b0c119bc58f5d85649fb2502eb402775cc4cce11ae2cb66dac1f7a93aba535f`.
- No uploaded raw request/response, image payload, keys or headers saved by the probe; only source hashes, approved numeric contour evidence and safe statuses/errors.

Workers-best-practices and Wrangler skills guided binding reuse, explicit fail-open boundaries and the non-deploying dry-run. Native binding development behavior was checked against [Cloudflare Wrangler API documentation](https://developers.cloudflare.com/workers/wrangler/api/).

BLOCKED: 좌표계가 일관된 direct face geometry의 유효한 3-case 품질 증거 미확보
